- name: ping the play
  hosts: web
  tasks:
  - name: pinging the play
    ansible.builtin.ping:


- name: installing nginx
  hosts: web
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present