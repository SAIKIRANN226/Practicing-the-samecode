- name: nginx server
  hosts: localhost
  become: yes
  tasks:
  - name: Installing nginx
    ansible.builtin.package:
        name: nginx
        state: present
  - name: Run the nginx
    ansible.builtin.service:
        name: nginx
        state: started