- name: ping the play
  hosts: localhost
  tasks:
  - name: pinging the play
    ansible.builtin.ping:


- name: installing nginx
  hosts: localhost
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present